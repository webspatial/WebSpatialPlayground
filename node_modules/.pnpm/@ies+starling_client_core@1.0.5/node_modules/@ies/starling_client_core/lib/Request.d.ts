/**
 * @author fengweiyao
 * 模块负责与远端进行交互，与 localCache 提供相同的对外 api。
 */
import { Options, Texts } from './options';
export declare type RequestOptions = Required<Pick<Options, 'request' | 'timeout' | 'apiKey' | 'locale' | 'fallbackLang' | 'zoneHost' | 'cdn' | 'mode'>> & {
    getReleaseVersion: (ns: string) => string;
};
interface TextResponse {
    updated: boolean;
    data?: Texts;
    version?: string;
    lang?: string;
}
export declare class Request {
    options: RequestOptions;
    constructor(options: RequestOptions);
    getText(namespace: string, localVersion: string): Promise<TextResponse>;
    getTexts(namespaces: Array<[string, string]>): Promise<Array<TextResponse>>;
    private _request;
    private getUrl;
    private _mapResponse;
}
export {};
