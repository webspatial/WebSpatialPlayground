/**
 * @author fengweiyao
 * 模块负责与本地缓存进行交互，对外提供和 Request 相同的 api。
 */
import { Options, Texts, trackEvent } from './options';
export declare type CacheOptions = Required<Pick<Options, 'apiKey' | 'locale' | 'store' | 'cache' | 'cdn'>> & {
    tracker?: trackEvent;
    getReleaseVersion: (ns: string) => string;
};
export declare class LocalCache {
    options: CacheOptions;
    constructor(options: CacheOptions);
    private _setCache;
    private _getCache;
    getVersion(namespace: string): any;
    getVersions(namespaces: Array<string>): Promise<Array<string>>;
    getText(namespace: string): any;
    getTexts(namespaces: Array<string>): Promise<Array<Texts>>;
    refresh(namespaces: string[], textRes: Array<Texts>, newVers: string[], langArr: string[]): Promise<Texts>;
}
