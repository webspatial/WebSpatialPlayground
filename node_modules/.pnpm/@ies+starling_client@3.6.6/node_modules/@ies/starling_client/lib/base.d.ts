import Starling from '@ies/starling_client_core';
import { Options } from '@ies/starling_client_core/lib/options';
declare type WithOptional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
export declare type WebOptions = WithOptional<Options, 'store' | 'request'>;
declare class StarlingWebBase extends Starling {
    constructor(options: WebOptions);
}
export default StarlingWebBase;
