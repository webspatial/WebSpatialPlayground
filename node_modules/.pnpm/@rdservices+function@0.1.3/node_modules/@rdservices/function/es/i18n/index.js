var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { I18n } from '@ies/starling_intl';
/**
 * 初始化 I18n 实例
 * @param resources - 国际化文案
 * @param curLang - 当前语言，默认中文
 * @param opts - \@ies/starling_intl 中 init 方法的配置项
 *
 */
export var i18nInit = function (resources, curLang, opts) {
    if (curLang === void 0) { curLang = 'zh'; }
    opts = __assign({ initImmediate: false, keySeparator: false, resources: resources, fallbackLng: ['zh', 'en'], lng: curLang }, opts);
    I18n.init(opts);
};
/**
 * i18n['文案 Key']({ 插值Key: 插值Val }, '兜底文案')
 * @returns {string} 国际化文案
 */
export var i18n = new Proxy({}, {
    get: function (_target, p) {
        return function (params, _defaultMes) {
            return I18n.t(p, params);
        };
    },
});
/**
 * 获取当前语言
 * @returns {string} 当前语言
 */
export var getCurLang = function () {
    return I18n.getLanguages()[0];
};
/**
 * 设置当前语言
 * @returns {void}
 */
export var setCurLang = function (lng) {
    return I18n.setLang(lng);
};
/**
 * starling I18n 实例
 */
export var starlingInstance = I18n;
