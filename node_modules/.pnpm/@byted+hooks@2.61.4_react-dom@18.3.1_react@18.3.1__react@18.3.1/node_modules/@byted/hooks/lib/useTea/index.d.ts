import type Tea from 'byted-tea-sdk';
declare type Options = {
    channel?: "cn" | "va" | "sg" | undefined;
    log?: boolean;
    test?: boolean;
    evtParams?: {
        [key: string]: any;
    };
    configParams?: {
        [key: string]: any;
    };
    ifMountToWindow?: boolean;
    sendFnName?: string;
};
declare type Result = {
    send: any;
    loaded: boolean;
    error: ErrorEvent | null;
};
declare global {
    interface Window {
        collectEvent: <T extends "init" | "config" | "start">(f: T, ...args: Parameters<Tea[T]>) => unknown;
    }
}
declare const useTea: (appId: number, cdnUrl: string, options?: Options) => Result;
export default useTea;
