import { IBoolean } from './types';
declare class CBoolean implements IBoolean {
    constructor(v?: boolean);
    private value;
    setTrue(): void;
    setFalse(): void;
    isFalse(): boolean;
    isTrue(): boolean;
}
export default CBoolean;
