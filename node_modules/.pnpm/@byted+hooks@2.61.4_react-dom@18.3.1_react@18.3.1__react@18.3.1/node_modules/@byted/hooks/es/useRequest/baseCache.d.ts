export declare type CachedKeyType = string;
export declare type CacheStoreType = 'memory' | 'localStorage' | 'sessionStorage';
declare type storageType = Record<CachedKeyType, CachedData>;
export interface CachedData<T = any> {
    /** 缓存数据 */
    data: T;
    /** 缓存过期时间 */
    expireTime: number;
    /** 缓存保存时间 */
    startTime: number;
}
declare class BaseCache {
    protected memoryValue: Map<CachedKeyType, CachedData>;
    protected localStorageValue: storageType;
    protected sessionStorageValue: storageType;
    constructor();
    getValue: (key: CachedKeyType, cacheStoreType: CacheStoreType) => CachedData | undefined;
    setValue: (key: CachedKeyType, data: CachedData, cacheStoreType: CacheStoreType) => void;
    removeItem: (key: CachedKeyType, cacheStoreType: CacheStoreType) => void;
    /**
     * 同步缓存到Storage
     * @param storage
     * @param data
     * @param isRetry
     */
    private syncStorage;
    /**
     * 压缩缓存对象
     * @param data
     * @returns
     */
    private compressStorage;
}
export default BaseCache;
