function getAllFiberChildren(fiber) {
    const allChildren = [];
    let { child } = fiber;
    while(child){
        allChildren.push(child);
        child = child.sibling;
    }
    return allChildren;
}
export { getAllFiberChildren };
