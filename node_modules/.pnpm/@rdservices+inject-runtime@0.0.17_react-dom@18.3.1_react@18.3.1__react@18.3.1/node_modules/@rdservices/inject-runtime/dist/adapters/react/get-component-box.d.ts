import { Fiber } from '../../types/react';
import { SimpleDOMRect } from '../../types/types';
export declare function getComponentBox(fiber: Fiber): {
    component: Fiber;
    componentBox: SimpleDOMRect;
};
