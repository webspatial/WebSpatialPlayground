function getElementInfo(found) {
    const filePath = found.getAttribute('data-aime-path-name');
    if (filePath) {
        const componentBox = found.getBoundingClientRect();
        const label = found.getAttribute('data-aime-component-name') ?? '';
        const line = Number(found.getAttribute('data-aime-line'));
        const columnNumber = Number(found.getAttribute('data-aime-column'));
        return {
            thisElement: {
                box: componentBox,
                label,
                link: {
                    filePath,
                    line,
                    column: columnNumber
                }
            },
            htmlElement: found,
            componentBox
        };
    }
    if (found.parentElement) return getElementInfo(found.parentElement);
    return null;
}
function getParentsPaths() {
    return [];
}
const domAttributeAdapter = {
    getElementInfo,
    getParentsPaths
};
export { domAttributeAdapter, getElementInfo };
