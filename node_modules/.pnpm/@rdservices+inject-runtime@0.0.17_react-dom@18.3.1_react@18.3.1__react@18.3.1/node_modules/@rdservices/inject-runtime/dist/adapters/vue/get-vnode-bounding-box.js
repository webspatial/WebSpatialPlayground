import { mergeRects } from "../../utils/merge-rects.js";
function getVueComponentBoundingBox(vcomponent) {
    let composedRect = null;
    if (vcomponent?.subTree?.children && vcomponent?.subTree?.children instanceof Array) vcomponent?.subTree?.children.forEach((child)=>{
        const box = getVNodeBoundingBox(child);
        if (!box) return;
        if (box.width <= 0 || box.height <= 0) return;
        composedRect = composedRect ? mergeRects(composedRect, box) : box;
    });
    return composedRect;
}
function getVNodeBoundingBox(vnode) {
    if (vnode.el instanceof HTMLElement) return vnode.el.getBoundingClientRect();
    if (vnode.component) return getVueComponentBoundingBox(vnode.component);
    return null;
}
export { getVNodeBoundingBox, getVueComponentBoundingBox };
